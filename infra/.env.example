# Paths are resolved from this infra/ directory where docker compose is executed.
# Change these if your backend/frontend repositories have different names or locations.
BACKEND_DIR=../../path-to-backend
FRONTEND_DIR=../../path-to-frontend
